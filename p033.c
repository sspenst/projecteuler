/*#include <stdio.h>
#include <stdlib.h>

int main() {
	int i, j;

	for (i = 10; i <= 99; i++) {
		for (j = 10; j <= 99; j++)  {
			if (((double)i / (double)j == (double)(i / 10) / (double)(j % 10)) && (i % 10 == j / 10) && (i % 11 != 0))
				printf("%d / %d = %d / %d\n", i, j, i / 10, j % 10);
		}
	}

	// (16*19*26*49)/(64*95*65*98) = 1/100

	system("pause");
	return 0;
}*/